name: Migrate Database (manual)

# One-time, MANUALLY TRIGGERED migration of the payroll database from the
# native Postgres cluster into the dockerized stack. The native-cluster
# cutover already ran on Lightsail; this workflow now SSHs to the AIC VPS
# so leftover stages (inspect/rollback) still have a host after Lightsail
# is decommissioned. There is NO push/schedule trigger.
#
# Runs deploy/migrate-database.sh on the VPS over the same pinned-host-key SSH
# path as the deploy workflow, using secrets.VPS_SSH_PRIVATE_KEY from the
# "production" environment. The database dump never leaves the VPS.

on:
  workflow_dispatch:
    inputs:
      stage:
        description: "Migration stage to run (run in order; stop to review between each)"
        required: true
        type: choice
        options:
          - discover
          - backup
          - restore
          - verify
          - inspect
          - cutover
          - rollback

permissions:
  contents: read

concurrency:
  group: payroll-migration
  cancel-in-progress: false

env:
  VPS_HOST: 162.19.81.122
  VPS_PORT: "20049"
  VPS_USER: root
  VPS_HOST_KEY: "[162.19.81.122]:20049 ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIIxenH95jfg3hxfkumJpZJyV7phwCNDrXrAL0wUXnta9"

jobs:
  migrate:
    name: "Run migration stage: ${{ inputs.stage }}"
    runs-on: ubuntu-latest
    timeout-minutes: 30
    environment: production

    steps:
      - name: Check out repo
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Configure pinned SSH access
        shell: bash
        env:
          VPS_SSH_PRIVATE_KEY: ${{ secrets.VPS_SSH_PRIVATE_KEY }}
        run: |
          set -euo pipefail
          test -n "$VPS_SSH_PRIVATE_KEY"
          install -m 0600 /dev/null "$RUNNER_TEMP/vps-key"
          printf '%s\n' "$VPS_SSH_PRIVATE_KEY" > "$RUNNER_TEMP/vps-key"
          install -m 0600 /dev/null "$RUNNER_TEMP/vps-known-hosts"
          printf '%s\n' "$VPS_HOST_KEY" > "$RUNNER_TEMP/vps-known-hosts"
          ssh-keygen -lf "$RUNNER_TEMP/vps-known-hosts" \
            | grep -Fq 'SHA256:HV6OszvuYmwmWRLEY7z7jm8GojWZJppJlDBHIhJVUxw'

      - name: Stage migration scripts onto the VPS
        shell: bash
        run: |
          set -euo pipefail
          ssh_opts=(
            -i "$RUNNER_TEMP/vps-key"
            -p "$VPS_PORT"
            -o BatchMode=yes
            -o ConnectTimeout=15
            -o IdentitiesOnly=yes
            -o StrictHostKeyChecking=yes
            -o "UserKnownHostsFile=$RUNNER_TEMP/vps-known-hosts"
          )
          scp_opts=(
            -i "$RUNNER_TEMP/vps-key"
            -P "$VPS_PORT"
            -o BatchMode=yes
            -o ConnectTimeout=15
            -o IdentitiesOnly=yes
            -o StrictHostKeyChecking=yes
            -o "UserKnownHostsFile=$RUNNER_TEMP/vps-known-hosts"
          )
          scp "${scp_opts[@]}" \
            deploy/deploy.sh deploy/migrate-database.sh deploy/docker-compose.prod.yml \
            "$VPS_USER@$VPS_HOST:/tmp/"
          ssh "${ssh_opts[@]}" "$VPS_USER@$VPS_HOST" \
            "set -eu
             sudo install -d -m 0750 /opt/payroll /opt/payroll/migration
             sudo install -m 0755 /tmp/deploy.sh /tmp/migrate-database.sh /opt/payroll/migration/
             sudo install -m 0644 /tmp/docker-compose.prod.yml /opt/payroll/migration/
             rm -f /tmp/deploy.sh /tmp/migrate-database.sh /tmp/docker-compose.prod.yml"

      - name: Run stage on the VPS
        shell: bash
        env:
          STAGE: ${{ inputs.stage }}
        run: |
          set -euo pipefail
          ssh_opts=(
            -i "$RUNNER_TEMP/vps-key"
            -p "$VPS_PORT"
            -o BatchMode=yes
            -o ConnectTimeout=15
            -o IdentitiesOnly=yes
            -o ServerAliveInterval=15
            -o ServerAliveCountMax=8
            -o StrictHostKeyChecking=yes
            -o "UserKnownHostsFile=$RUNNER_TEMP/vps-known-hosts"
          )
          # All stages run as root from the staged migration script. cutover
          # self-locates the newest CI release dir (it runs as root and can
          # read the 0750 releases tree), so no special-casing is needed here.
          ssh "${ssh_opts[@]}" "$VPS_USER@$VPS_HOST" \
            "sudo /bin/bash /opt/payroll/migration/migrate-database.sh $STAGE"

      - name: Remove ephemeral SSH key
        if: always()
        shell: bash
        run: rm -f "$RUNNER_TEMP/vps-key" "$RUNNER_TEMP/vps-known-hosts"
